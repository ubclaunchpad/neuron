# Test environment configuration
# Uses the same PostgreSQL instance but a different database (neuron_test)

NODE_ENV=test
SKIP_ENV_VALIDATION=true

# Test Database (same host, different database name)
DATABASE_PASSWORD="0d9aQECGfwiKdFDL"
DATABASE_URL="postgresql://postgres:0d9aQECGfwiKdFDL@localhost:5432/neuron_test"

# Redis (not used in tests, but required by env validation)
REDIS_PASSWORD="zRCjoHSI6Ywrf3SZ"
REDIS_URL="redis://:zRCjoHSI6Ywrf3SZ@localhost:6379/0"

# Minio (mocked in tests)
MINIO_ROOT_USER="testadmin"
MINIO_ROOT_PASSWORD="testpassword"
MINIO_HOST="localhost"
MINIO_PORT="9000"
MINIO_BUCKET="neuron-test"
MINIO_USE_SSL="false"
FILES_BASE_URL="http://localhost:9000"
FILES_BUCKET="neuron-test"

# Email (mocked in tests)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=test
SMTP_PASS=test
MAIL_FROM="Test <test@neuron.test>"
